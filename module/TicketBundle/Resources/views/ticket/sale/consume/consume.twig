{% extends 'ticket/base-consume.twig' %}

{% block content %}
    <div class="container modal-body">
        {% import 'site/partials/form.twig' as forms %}
        {{ forms.renderForm(form) }}
        {% if noEntity != null %}
            <div class="flashmessage alert alert-danger " id="ticket_sale_consume_error" >
                <a class="close" data-dismiss="alert">&times;</a>
                <div class="content">
                    <h2>Geen bonnen gevonden voor deze persoon!</h2>
                </div>
            </div>
        {% endif %}
        {% if msg != null %}
            <div class="flashmessage alert alert-danger " id="ticket_sale_consume_error" >
                <a class="close" data-dismiss="alert">&times;</a>
                <div class="content">
                    <h1>Niet genoeg bonnen over voor {{ name }}!</h1>
                    <h2>Bonnen over: {{ amount }}</h2>
                </div>
            </div>
        {% endif %}
        {% if amount_left != null  %}
            <div class="flashmessage alert alert-success " id="ticket_sale_consume_success" >
                <a class="close" data-dismiss="alert">&times;</a>
                <div class="content">
                    <h2>Bonnen over voor {{ name }}: {{ amount_left }}</h2>
                </div>
            </div>
        {% endif %}
        {% if empty != null %}
            <div class="flashmessage alert alert-success " id="ticket_sale_consume_success" >
                <a class="close" data-dismiss="alert">&times;</a>
                <div class="content">
                    <h2>{{ name}} heeft nu alle bonnen opgebruikt!</h2>
                </div>
            </div>
        {% endif %}
    </div>

{% endblock %}

{% block content_script %}
<script type="text/javascript">
    $(document).ready(function (){
        document.getElementById('username').focus();
        $('#username').keydown( function(e) {
            if (e.key === 'Enter') {
                e.preventDefault()
                document.getElementById('amount').focus();
            }
        });
    });
</script>
{% endblock %}


