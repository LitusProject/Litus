<header id="controller_sub_header">
    <nav id="controller_sub_nav">
        <ul id="supplier_nav">
            {% for academicYear in academicYears %}
                <li><a {% if academicYear.getCode() == currentAcademicYear.getCode() %} class="active" {% endif %}href="{{ url("admin_syllabus_academic", {"action": "edit", "id": academic.getId(), "academicyear": academicYear.getCode()}) }}">{{ academicYear.getCode() }}</a></li>
            {% endfor %}
        </ul>
    </nav>
</header>
