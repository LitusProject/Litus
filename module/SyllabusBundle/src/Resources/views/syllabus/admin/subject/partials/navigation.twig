<header id="controller_header">
    <div class="title"><span class="study">Subjects</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_subject', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_subject') }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
