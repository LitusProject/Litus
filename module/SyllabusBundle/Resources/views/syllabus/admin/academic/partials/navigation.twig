<header id="controller_header">
    <div class="title"><span class="academic">Academic</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('syllabus_admin_academic', 'manage') %}
                <li>
                    <a
                        {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %}
                        href="{{ url('syllabus_admin_academic', {"action": "manage"}) }}"
                    >
                        Manage
                    </a>
                </li>
            {% endif %}
        </ul>
    </nav>
</header>
