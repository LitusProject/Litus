<ul class="nav nav-tabs" id="navigation">
    {% if hasAccess('admin_page', 'manage') %}
        <li {% if 'manage' == getParam('action') %}class="active"{% endif %}>
            <a href="{{ url("admin_page", {"action": "manage"}) }}">{{ translator().translate('Manage') }}</a>
        </li>
    {% endif %}
    {% if hasAccess('admin_page', 'add') %}
        <li {% if 'add' == getParam('action') %}class="active"{% endif %}>
            <a href="{{ url("admin_page", {"action": "add"}) }}">{{ translator().translate('Add') }}</a>
        </li>
    {% endif %}
</ul>