<header id="controller_header">
    <div class="title"><span class="unit">Units</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_unit', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_unit", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_unit', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_unit", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
