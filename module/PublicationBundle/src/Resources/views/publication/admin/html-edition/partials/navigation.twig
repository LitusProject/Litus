<header id="controller_header">
    <div class="title"><span class="html">HTML Editions</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_edition_html', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_edition_html', {"action": "add", "id": publication.getId()}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_edition_html', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_edition_html', {"action": "manage", "id": publication.getId()}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
