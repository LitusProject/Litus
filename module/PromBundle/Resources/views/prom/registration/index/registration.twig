{% extends 'prom/base.twig' %}

{% block content %}
    <div class="page-header">
        You can use this little application to reserve a seat on a bus.<br> You can also manage your reservation if you want to make changes.
    </div>

    {% if enable %}
        <div class="row">
            <div class="col-md-6">
                <h2>New Registration</h2>
                {% import 'site/partials/form.twig' as forms %}
                {{ forms.renderForm(createForm) }}
            </div>
            <div class="col-md-6">
                <h2>Manage Registration</h2>
                {{ forms.renderForm(manageForm) }}
            </div>
        </div>
    {% else %}
        <h2>At the moment, the bus reservation application is still closed.</h2>
    {% endif %}
{% endblock %}
