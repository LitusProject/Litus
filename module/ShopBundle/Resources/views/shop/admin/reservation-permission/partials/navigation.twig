<header id="controller_header">
    <div class="title"><span class="shift">Reservation permissions</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('shop_admin_shop_reservationpermission', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %}
                            href="{{ url("shop_admin_shop_reservationpermission", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('shop_admin_shop_reservationpermission', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %}
                            href="{{ url("shop_admin_shop_reservationpermission", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
            {% if hasAccess('shop_admin_shop_reservationpermission', 'old') %}
                <li><a {% if 'old' == getParam('action') %} class="active" {% endif %}
                            href="{{ url("shop_admin_shop_reservationpermission", {"action": "old"}) }}">Old</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
