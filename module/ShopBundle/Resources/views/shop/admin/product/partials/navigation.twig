<header id="controller_header">
    <div class="title"><span class="shift">Products</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('shop_admin_shop_product', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("shop_admin_shop_product", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('shop_admin_shop_product', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url("shop_admin_shop_product", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
