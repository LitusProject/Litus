{% for flashMessage in flashMessenger %}
    <div class="flashmessage alert alert-{{ flashMessage.getType() }} fade in">
        <a class="close" data-dismiss="alert">&times;</a>
        {{ translate(flashMessage.getMessage())|raw }}
    </div>
{% endfor %}
