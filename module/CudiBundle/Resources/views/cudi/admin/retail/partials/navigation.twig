<header id="controller_header">
    <div class="title"><span class="stock">Retail</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('cudi_admin_retail', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('cudi_admin_retail', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('cudi_admin_retail', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('cudi_admin_retail', {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>