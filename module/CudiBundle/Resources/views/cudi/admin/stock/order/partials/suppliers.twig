<header class="controller_sub_header">
    <nav class="controller_sub_nav">
        <ul>
            {% for supp in suppliers %}
                <li><a {% if supp.getId() == supplier.getId() %} class="active" {% endif %}href="{{ url("cudi_admin_stock_order", {"action": "supplier", "id": supp.getId()}) }}">{{ supp.getName() }}</a></li>
            {% endfor %}
        </ul>
    </nav>
</header>
