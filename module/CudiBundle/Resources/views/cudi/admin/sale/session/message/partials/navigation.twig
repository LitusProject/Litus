<header id="controller_header">
    <div class="title"><span class="clock">Message</span></div>
    <nav id="controller_nav">
        <ul>
            {% if hasAccess('cudi_admin_sales_session_message', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('cudi_admin_sales_session_message', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('cudi_admin_sales_session_message', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url('cudi_admin_sales_session_message', {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
