<?php

namespace CudiBundle\Repository\Log\Articles\Sales;

use DateTime,
    Doctrine\ORM\EntityRepository;

/**
 * Bookable
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class Bookable extends EntityRepository
{
    public function findAllAfter(DateTime $date)
    {
        $query = $this->_em->createQueryBuilder();
        $resultSet = $query->select('l')
            ->from('CudiBundle\Entity\Log\Article\Sale\Bookable', 'l')
            ->where(
                $query->expr()->gt('l.timestamp', ':date')
            )
            ->setParameter('date', $date)
            ->getQuery()
            ->getResult();

        return $resultSet;
    }
}
