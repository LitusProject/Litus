<header id="controller_header">
    <div class="title"><span class="article">Articles</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_article', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_article', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_article', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_article', {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>