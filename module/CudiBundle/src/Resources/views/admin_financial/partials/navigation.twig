<header id="controller_header">
    <div class="title"><span class="financial">Financial</span></div>
 
    <nav id="controller_nav">
        <ul>
        	{% if hasAccess('admin', 'financial', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_financial', {"action": "manage"}) }}">Manage</a></li>
			{% endif %}
        </ul>
    </nav>
</header>
