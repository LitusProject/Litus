<header id="controller_header">
    <div class="title"><span class="quiz">Quiz</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('quiz_admin_quiz', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('quiz_admin_quiz', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('quiz_admin_quiz', 'manage') %}
                <li>
                    <a
                        {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %}
                        href="{{ url('quiz_admin_quiz', {"action": "manage"}) }}"
                    >
                        Manage
                    </a>
                </li>
            {% endif %}
        </ul>
    </nav>
</header>
