{% extends 'quiz/base.twig' %}

{% block content %}
    <header class="subhead">
        <h1>{{ quiz.getName() }}</h1>
    </header>
    <ul class="nav nav-tabs" id="rounds_tab">
        {% for round in rounds %}
            <li>
                <a href="#round-{{ round.getId() }}" data-toggle="tab">{{ round.getName() }}</a>
            </li>
        {% endfor %}
    </ul>
    <div class="tab-content" id="rounds_tab-content">
        {% for round in rounds %}
            <div id="round-{{ round.getId() }}" class="tab-pane fade in form-horizontal">
                {% for team in teams %}
                    <div class="control-group">
                        <label class="control-label" for="input-{{ team.getId() }}-{{ round.getId() }}">{{ team.getName() }}</label>
                        <div class="controls">
                            <input type="number" min="0" max="{{ round.getMaxPoints() }}" step="1" id="input-{{ team.getId() }}-{{ round.getId() }}" data-team="{{ team.getId() }}" data-round="{{ round.getId() }}" class="input-xlarge roundScore" value="{% if points[team.getId()][round.getId()] is not defined %}0{% else %}{{ points[team.getId()][round.getId()] }}{% endif %}">
                            <span class="help-inline hide">The score couldn't be saved.</span>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>
{% endblock %}

{% block content_script %}
    <script>
    var ajax_requests_pending = 0;
    $(document).ready(function () {
        $('#rounds_tab li:first').addClass('active');
        $('#rounds_tab-content .tab-pane:first').addClass('active');

        $('.roundScore').keyup(function (e) {
            if (e.keyCode == 13) {
                var next = $(this).parent().parent().next().find('.roundScore');
                next.focus().select();
                if (next.find('.roundScore').length == 0)
                    $(this).blur();
            }
        }).blur(function () {
            var field = $(this);
            field.parent().parent().removeClass('success').removeClass('error').find('.help-inline').addClass('hide');
            if (parseInt(field.val(), 10) > parseInt(field.attr('max'),10)) {
                field.parent().parent().addClass('error');
                return;
            }
            ajax_requests_pending++;
            var url = '{{ url('quiz_quiz', {'action': 'update', 'quizid': quiz.getId()})}}'+$(this).data('round') + '/' + $(this).data('team')
            $.ajax({
                url: url,
                type: 'post',
                data: {score: $(this).val()},
                dataType: 'json',
                success: function () {
                    field.parent().parent().addClass('success');
                    ajax_requests_pending--;
                },
                error: function () {
                    field.parent().parent().addClass('error').find('.help-inline').removeClass('hide');
                    ajax_requests_pending--;
                }
            });
        });
    });

    $(window).on('beforeunload', function() {
        if(ajax_requests_pending > 0)
            return 'Not all scores have been saved. Are you sure you want to leave this page?';
    });
    </script>
{% endblock %}