<?php

namespace QuizBundle\Repository;

use QuizBundle\Entity\Quiz as QuizEntity;

/**
 * Point
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class Point extends \CommonBundle\Component\Doctrine\ORM\EntityRepository
{
    /**
     * Gets all points belonging to a quiz
     * @param QuizEntity $quiz The quiz the points must belong to
     */
    public function findAllByQuizQuery(QuizEntity $quiz)
    {
        $query = $this->getEntityManager()->createQueryBuilder();
        return $query->select('p')
            ->from('QuizBundle\Entity\Point', 'p')
            ->innerJoin('p.round', 'r')
            ->innerJoin('p.team', 't')
            ->where(
                $query->expr()->eq('r.quiz', ':quiz')
            )
            ->orderBy('r.order', 'ASC')
            ->setParameter('quiz', $quiz)
            ->getQuery();
    }
}
