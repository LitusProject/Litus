<header id="controller_header">
    <div class="title"><span class="form">Form</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_form', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_form", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_form', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_form", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
