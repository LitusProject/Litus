<header id="controller_header">
    <div class="title"><span class="calendar">Calendar</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_calendar', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_calendar', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_calendar', 'manage') %}
                <li>
                	<a
                		{% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %}
                		href="{{ url('admin_calendar', {"action": "manage"}) }}"
                	>
                		Manage
                	</a>
                </li>
            {% endif %}
        </ul>
    </nav>
</header>
