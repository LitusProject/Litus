<ul class="nav nav-tabs" id="navigation">
    {% if hasAccess('admin_calendar', 'manage') %}
        <li {% if 'manage' == getParam('action') %}class="active"{% endif %}>
            <a href="{{ url("admin_calendar", {"action": "manage"}) }}">{{ translator().translate('Manage') }}</a>
        </li>
    {% endif %}
    {% if hasAccess('admin_calendar', 'add') %}
        <li {% if 'add' == getParam('action') %}class="active"{% endif %}>
            <a href="{{ url("admin_calendar", {"action": "add"}) }}">{{ translator().translate('Add') }}</a>
        </li>
    {% endif %}
</ul>