{% extends 'logistics/base.twig' %}
{% import 'site/partials/form.twig' as forms %}

{% block content %}
    <h1>{{ translate('Edit Order') }}</h1>
    <div>{{ translate("If you don't see your location as a possible option, put in in the description (be as complete as possible). ") }}</div>
    <br>
    <h4>{{ translate('Name') }}: <b>{{ order.getName() }}</b></h4>
    <br>

    {{ forms.renderForm(form) }}
{% endblock %}

