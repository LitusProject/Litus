<header id="controller_header">
    <div class="title"><span class="reservation">Van Reservations</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_vanreservation', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_vanreservation', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_vanreservation', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_vanreservation', {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
