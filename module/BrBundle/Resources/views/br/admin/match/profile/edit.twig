{% extends 'admin/base.twig' %}

{% block content %}
    {% include 'br/admin/match/partials/navigation.twig' %}
    {% include 'br/admin/match/profile/partials/navigation.twig' %}

    {% include 'admin/partials/flashMessenger.twig' %}


    <div id="controller_action">
        {% import 'admin/partials/form.twig' as forms %}
        <h1>Edit</h1>
        <h2>{{ profile.getUserName(em) | title }}</h2>
        <h2>{{ profile.getProfileType() | title }} {{ translate('Profile') }}</h2>
        <br>
        <b><p>You can distribute maximum {{ sector_points }} points for the sector features! </p></b>
        <br>
        {{ forms.renderForm(form) }}
    </div>

{% endblock %}