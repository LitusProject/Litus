<header class="controller_sub_header">
    <div class="title"><span class="feature">Matching Waves</span></div>
    <nav class="controller_sub_nav" style="float: right">
        <ul>
            {% if hasAccess('br_admin_match_wave', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('br_admin_match_wave', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('br_admin_match_wave', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('br_admin_match_wave', {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
