<header class="controller_sub_header">
    <nav class="controller_sub_nav">
        <ul>
            {% if hasAccess('br_admin_event_subscription', 'overview') %}
                <li><a {% if 'overview' == getParam('action') %} class="active" {% endif %} href="{{ url('br_admin_event_subscription', {"action": "overview", "event": event.getId()}) }}">Overview</a></li>
            {% endif %}
            {% if hasAccess('br_admin_event_subscription', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('br_admin_event_subscription', {"action": "add", "event": event.getId()}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('br_admin_event_subscription', 'csv') %}
                <li><a {% if 'csv' == getParam('action') %} class="active" {% endif %} href="{{ url('br_admin_event_subscription', {"action": "csv", "event": event.getId()}) }}">CSV</a></li>
            {% endif %}
            {% if hasAccess('br_admin_event_subscription', 'reminder') %}
                <li><a {% if 'reminder' == getParam('action') %} class="active" {% endif %} href="{{ url('br_admin_event_subscription', {"action": "reminder", "event": event.getId()}) }}">Mail All</a></li>
            {% endif %}
        </ul>
    </nav>
</header>