{% extends 'career/partials/layout.twig' %}

{% block page_content %}

    <div class="row">
        <a class="button blue pull-right" style="margin-left: 20px;margin-top: 10px;" href="{{ url('br_career_match') }}">HOME</a>

        <a class="button blue unfilled pull-right" style="margin-left: 20px;margin-top: 10px;" href="{{ url('br_career_match', {'action': 'editProfile', 'type': type }) }}">Edit Profile</a>

        {% if type == 'company' %}
            <h1 style="float:left;">
                {{ translate('View Company Profile') }}
            </h1>
            <a class="button blue unfilled pull-right" style="margin-left: 100px;margin-top: 10px;" href="{{ url('br_career_match', {'action': 'viewProfile', 'type': 'student' }) }}">View Student Profile</a>
        {% elseif type == 'student' %}
            <h1 style="float:left;">
                {{ translate('View Student Profile') }}
            </h1>
            <a class="button blue unfilled pull-right" style="margin-left: 100px;margin-top: 10px;" href="{{ url('br_career_match', {'action': 'viewProfile', 'type': 'company' }) }}">View Company Profile</a>
        {% endif %}
    </div>
    <div class="row">
        {% if type == 'student' %}
            <div style="margin-top: 15px;"><strong>{{ translate('This profile should describe what you are like as a student.') }}</strong>
                <p>{{ translate('A company will have a higher match rating, if your personal traits matches the traits that they are looking for!') }}</p></div>
        {% elseif type == 'company' %}
            <div style="margin-top: 15px;"><strong>{{ translate('This profile should describe what you are looking for in a company.') }}</strong>
                <p>{{ translate('A company will have a higher match rating, if the company matches the traits that you are looking for!') }}</p></div>
        {% endif %}
    </div>

    <div style="height: 40px"></div>

    <div class="vtk-flex-stretch">
        <div class="col-md-6" style="margin:auto;">
            <h4>Features</h4>
            <table class="table table-striped full-width">
                {% for feature in features %}
                    <tr>
                        <td>
                            <strong>{{ feature.getFeature().getName() }}</strong>
                        </td>
                    </tr>
                {% endfor %}
            </table>

            {% include 'site/partials/paginationControl.twig' %}
        </div>
    </div>

{% endblock %}
