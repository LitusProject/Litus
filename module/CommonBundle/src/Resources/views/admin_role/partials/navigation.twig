<header id="controller_header">
    <div class="title"><span class="groups">Groups</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin', 'role', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_role', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin', 'role', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_role', {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>