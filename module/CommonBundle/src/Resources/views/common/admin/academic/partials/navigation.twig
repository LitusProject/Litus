<header id="controller_header">
    <div class="title"><span class="academic">Academic</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('common_admin_academic', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url('common_admin_academic', {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('common_admin_academic', 'manage') %}
                <li>
                	<a
                		{% if 'manage' == getParam('action') or 'edit' == getParam('action') or 'delete' == getParam('action') %} class="active" {% endif %}
                		href="{{ url('common_admin_academic', {"action": "manage"}) }}"
                	>
                		Manage
                	</a>
                </li>
            {% endif %}
        </ul>
    </nav>
</header>
