<header id="controller_header">
    <div class="title"><span class="page">FAQs</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('common_admin_faq', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("common_admin_faq", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('common_admin_faq', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url("common_admin_faq", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
