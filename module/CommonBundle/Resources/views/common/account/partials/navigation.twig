<ul class="nav nav-tabs">
    {% if hasAccess('common_account', 'index') %}
        <li>
            <a style="margin-left: 10px" class="{% if getParam('action') == 'index' %} activeTab {% else %} tab {% endif %}" href="{{ url('common_account', {'action': 'index'}) }}">
                {{ translate('My VTK') }}
            </a>
        </li>
    {% endif %}

    {% if hasAccess('common_account', 'profile') %}
        <li>
            <a class="{% if getParam('action') == 'profile' %} activeTab {% else %} tab {% endif %}" href="{{ url('common_account', {'action': 'profile'}) }}">
                {{ translate('Profile') }}
            </a>
        </li>
    {% endif %}

    {% if hasAccess('cudi_booking', 'view') %}
        <li>
            <a class="tab" href="{{ url('cudi_booking', {'action': 'view'}) }}">
                {{ translate('Cudi') }}
            </a>
        </li>
    {% endif %}

    {% if hasAccess('shift', 'index') %}
        <li>
            <a class="tab" href="{{ url('shift', {'action': 'index'}) }}">
                {{ translate('Shifts') }}
            </a>
        </li>
    {% endif %}

    {% if hasAccess('br_career_match', 'overview') %}
        <li>
            <a class="tab" href="{{ url('br_career_match', {'action': 'overview'}) }}">
                {{ translate('Matching Software') }}
            </a>
        </li>
    {% endif %}
</ul>
