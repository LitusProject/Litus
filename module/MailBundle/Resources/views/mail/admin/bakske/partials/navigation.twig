<header id="controller_header">
    <div class="title"><span class="mail">Het Bakske</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('mail_admin_bakske', 'send') %}
                <li><a{% if 'send' == getParam('action') %} class="active" {% endif %} href="{{ url('mail_admin_bakske', {'action': 'send'}) }}">Send</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
