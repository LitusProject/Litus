<header id="controller_header">
    <div class="title"><span class="mail">Sections</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('mail_admin_section', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("mail_admin_section", {"action": "add"}) }}">Add Section</a></li>
            {% endif %}
            {% if hasAccess('mail_admin_section', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url("mail_admin_section", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>