<header id="controller_header">
    <div class="title"><span class="mail">Lists</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_mail_list', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_mail_list", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_mail_list', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_mail_list", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
