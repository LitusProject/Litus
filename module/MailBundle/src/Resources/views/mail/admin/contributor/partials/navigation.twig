<header id="controller_header">
    <div class="title"><span class="mail">Contributor Mail</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('mail_admin_mail_contributor', 'send') %}
                <li><a{% if 'send' == getParam('action') %} class="active" {% endif %} href="{{ url('mail_admin_mail_contributor', {'action': 'send'}) }}">Contributor</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
