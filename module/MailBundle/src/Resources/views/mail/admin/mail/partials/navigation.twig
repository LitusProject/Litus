<header id="controller_header">
    <div class="title"><span class="mail">Groups</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_mail', 'groups') %}
                <li><a{% if 'groups' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_mail') }}">Groups</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
