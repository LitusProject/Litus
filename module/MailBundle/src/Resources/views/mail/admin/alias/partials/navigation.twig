<header id="controller_header">
    <div class="title"><span class="mail">Aliases</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_mail_alias', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_mail_alias", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_mail_alias', 'manage') %}
                <li><a {% if 'manage' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_mail_alias", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
