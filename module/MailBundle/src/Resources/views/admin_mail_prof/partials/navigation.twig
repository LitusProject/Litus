<header id="controller_header">
    <div class="title"><span class="mail">Mail Prof</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_mail_prof', 'cudi') %}
                <li><a{% if 'cudi' == getParam('action') %} class="active" {% endif %} href="{{ url('admin_mail_prof', {'action': 'cudi'}) }}">Start Cudi</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
