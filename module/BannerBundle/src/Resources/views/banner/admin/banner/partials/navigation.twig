<header id="controller_header">
    <div class="title"><span class="banner">Banners</span></div>

    <nav id="controller_nav">
        <ul>
            {% if hasAccess('admin_banner', 'add') %}
                <li><a {% if 'add' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_banner", {"action": "add"}) }}">Add</a></li>
            {% endif %}
            {% if hasAccess('admin_banner', 'manage') %}
                <li><a {% if 'manage' == getParam('action') or 'edit' == getParam('action') %} class="active" {% endif %} href="{{ url("admin_banner", {"action": "manage"}) }}">Manage</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
