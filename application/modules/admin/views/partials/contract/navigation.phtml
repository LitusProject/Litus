<header id="controller_header">
    <div class="title"><span class="contracts">Contracts</span></div>

    <nav id="controller_nav">
        <ul>
            <? if ($this->hasAccess('admin', 'contract', 'add')): ?>
                <li><a <? if('add' == $this->vars('currentAction')) echo 'class="active"'; ?> href="<?= $this->url(array('action' => 'add')); ?>">Add</a></li>
            <? endif; ?>
            <? if ($this->hasAccess('admin', 'contract', 'manage')): ?>
                <li><a <? if('manage' == $this->vars('currentAction')) echo 'class="active"'; ?> href="<?= $this->url(array('action' => 'manage')); ?>">Manage</a></li>
            <? endif; ?>
            <? if ($this->hasAccess('admin', 'contract', 'download')): ?>
                <li><a <? if('download' == $this->vars('currentAction')) echo 'class="active"'; ?> href="<?= $this->url(array('action' => 'download')); ?>">Download</a></li>
            <? endif; ?>
        </ul>
    </nav>
</header>