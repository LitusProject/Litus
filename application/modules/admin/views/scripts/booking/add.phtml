<?= $this->partial('partials/booking/navigation.phtml', array('currentAction' => 'add')); ?>

<div id="controller_action">
    <?php foreach($this->flashMessages as $message) { echo $message; } ?>

	<?= $this->form; ?>
</div>