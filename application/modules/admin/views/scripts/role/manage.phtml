<?= $this->broker('partial')->direct('role/partials/navigation.phtml', array('currentAction' => 'manage')); ?>

<div id="controller_action">
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th width="80px">Action</th>
            </tr>
        </thead>

        <tbody>
            <? foreach ($this->paginator as $role): ?>
                <tr>
                    <td><?= $role->getName(); ?></td>
                    <td>Edit Delete</td>
                </tr>
            <? endforeach; ?>
        </tbody>
    </table>
</div>