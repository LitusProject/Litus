<?= $this->partial('partials/role/navigation.phtml', array('currentAction' => 'manage')); ?>

<div id="flashmessenger">
	<? foreach($this->flashMessages as $message) { echo $message; } ?>
</div>

<div id="controller_action">
	<table>
        <thead>
            <tr>
                <th>Name</th>
                <th width="115px">Action</th>
            </tr>
        </thead>

        <tbody>
            <? foreach ($this->paginator as $role): ?>
                <tr>
                    <td><?= $role->getName(); ?></td>
                    <td>
                        <a href="<?= $this->url(array('action' => 'edit', 'name' => $role->getName())); ?>" class="edit">Edit</a>
                        <a href="<?= $this->url(array('action' => 'delete', 'name' => $role->getName())); ?>" class="delete">Delete</a>
                    </td>
                </tr>
            <? endforeach; ?>
        </tbody>
    </table>

    <?= $this->paginationControl($this->paginator, 'all', 'partials/pagination.phtml'); ?>
</div>