<?= $this->partial('partials/stock/navigation.phtml', array('currentAction' => 'addorder')); ?>

<div id="controller_action">
    <? foreach($this->flashMessages as $message) { echo $message; } ?>

	<?= $this->form; ?>
</div>