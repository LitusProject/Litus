<xsl:stylesheet
    version="1.0"
    xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
>

    <xsl:template name="financial">
        <xsl:text>FINANCIEEL</xsl:text>
    </xsl:template>

    <xsl:template name="internal_articles">
        <xsl:text>Interne Artikels</xsl:text>
    </xsl:template>

    <xsl:template name="external_articles">
        <xsl:text>Externe Artikels</xsl:text>
    </xsl:template>

    <xsl:template name="title_author">
        <xsl:text>Titel (Auteur)</xsl:text>
    </xsl:template>

    <xsl:template name="publisher">
        <xsl:text>Uitgever</xsl:text>
    </xsl:template>

    <xsl:template name="barcode">
        <xsl:text>Barcode</xsl:text>
    </xsl:template>

    <xsl:template name="ordered">
        <xsl:text>Besteld</xsl:text>
    </xsl:template>

    <xsl:template name="delivered">
        <xsl:text>Geleverd</xsl:text>
    </xsl:template>

    <xsl:template name="sold">
        <xsl:text>Verkocht</xsl:text>
    </xsl:template>

    <xsl:template name="stock">
        <xsl:text>Stock</xsl:text>
    </xsl:template>

</xsl:stylesheet>
